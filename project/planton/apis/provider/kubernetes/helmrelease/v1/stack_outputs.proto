syntax = "proto3";

package project.planton.apis.provider.kubernetes.helmrelease;

//helm-release status on kubernetes cluster.
message HelmReleaseStackOutputs {
  //namespace in which the helm release has been created.
  string namespace = 1;
}

