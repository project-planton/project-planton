syntax = "proto3";

package project.planton.apis.provider.kubernetes.kuberneteshttpendpoint;

//kubernetes-http-endpoint stack outputs
message KubernetesHttpEndpointStackOutputs {
  //kubernetes namespace in which kubernetes-http-endpoint is deployed.
  string namespace = 1;
}
