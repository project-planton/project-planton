# =============================================================================
# Pulumi Modules - Confluent Provider
# =============================================================================
# Builds pre-compiled binaries for all Confluent Pulumi deployment components
# and uploads them to the GitHub Release created by release.yaml.
#
# Trigger: Push a version tag (v*)
#   Example: git tag v0.0.100 && git push origin v0.0.100
# =============================================================================

name: release.pulumi-modules-confluent

on:
  push:
    tags:
      - 'v*'

  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-confluent:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - component: confluentkafka
            path: apis/org/project_planton/provider/confluent/confluentkafka/v1/iac/pulumi

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache: true

      - name: Build and compress binary
        run: |
          BINARY_NAME="pulumi-${{ matrix.component }}"
          
          echo "=== Building ${BINARY_NAME} ==="
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build \
            -ldflags="-s -w" \
            -o "${BINARY_NAME}" \
            ./${{ matrix.path }}
          
          echo "=== Compressing with gzip ==="
          gzip -9 "${BINARY_NAME}"
          ls -lh "${BINARY_NAME}.gz"

      - name: Upload to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF#refs/tags/}"
          BINARY_NAME="pulumi-${{ matrix.component }}"
          
          # Wait for release to exist (GoReleaser needs a moment)
          echo "Waiting for release ${TAG}..."
          for i in {1..30}; do
            if gh release view "${TAG}" >/dev/null 2>&1; then
              echo "Release ${TAG} found!"
              break
            fi
            echo "  Attempt $i/30 - waiting 10s..."
            sleep 10
          done
          
          # Upload to the release
          echo "Uploading ${BINARY_NAME}.gz to release ${TAG}..."
          gh release upload "${TAG}" "${BINARY_NAME}.gz" --clobber
          echo "✓ Uploaded ${BINARY_NAME}.gz"

