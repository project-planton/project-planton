apiVersion: kubernetes.project-planton.org/v1
kind: KubernetesSolrOperator
metadata:
  name: solr-operator-example
spec:
  container:
    resources:
      requests:
        cpu: 50m
        memory: 100Mi
      limits:
        cpu: 1000m
        memory: 1Gi

