syntax = "proto3";

package org.project_planton.provider.kubernetes.addon.kubernetesperconamysqloperator.v1;

// kubernetes-percona-mysql-operator-kubernetes stack outputs
message KubernetesPerconaMysqlOperatorStackOutputs {
  // Namespace where the operator is installed
  string namespace = 1;
}
