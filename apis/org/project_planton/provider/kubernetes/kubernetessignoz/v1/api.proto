syntax = "proto3";

package org.project_planton.provider.kubernetes.kubernetessignoz.v1;

import "buf/validate/validate.proto";
import "org/project_planton/provider/kubernetes/kubernetessignoz/v1/spec.proto";
import "org/project_planton/provider/kubernetes/kubernetessignoz/v1/stack_outputs.proto";
import "org/project_planton/shared/metadata.proto";

// signoz-kubernetes
message KubernetesSignoz {
  // api-version
  string api_version = 1 [(buf.validate.field).string.const = 'kubernetes.project-planton.org/v1'];

  // resource-kind
  string kind = 2 [(buf.validate.field).string.const = 'KubernetesSignoz'];

  // metadata
  org.project_planton.shared.CloudResourceMetadata metadata = 3 [(buf.validate.field).required = true];

  // spec
  KubernetesSignozSpec spec = 4 [(buf.validate.field).required = true];

  // status
  KubernetesSignozStatus status = 5;
}

// signoz-kubernetes status
message KubernetesSignozStatus {
  // stack-outputs
  KubernetesSignozStackOutputs outputs = 1;
}
