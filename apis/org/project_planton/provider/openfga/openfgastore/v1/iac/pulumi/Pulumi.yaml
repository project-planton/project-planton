name: openfga-store
description: |
  OpenFGA Store deployment component.
  
  IMPORTANT: OpenFGA does not have a Pulumi provider. This module is a
  pass-through placeholder. Use Terraform/Tofu provisioner instead:
  
    project-planton apply --manifest openfga-store.yaml --provisioner tofu
runtime: go
