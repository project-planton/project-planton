name: openfga-authorization-model
description: |
  OpenFGA Authorization Model deployment component.
  
  IMPORTANT: OpenFGA does not have a Pulumi provider. This module is a
  pass-through placeholder. Use Terraform/Tofu provisioner instead:
  
    project-planton apply --manifest openfga-authorization-model.yaml --provisioner tofu
runtime: go
