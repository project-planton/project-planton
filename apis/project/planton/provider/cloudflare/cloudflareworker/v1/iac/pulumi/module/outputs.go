package module

const (
	// OpScriptId is the exported stack output containing the Worker script identifier.
	OpScriptId = "script_id"

	// OpRouteUrls is the exported stack output containing the route URL(s)/pattern(s).
	OpRouteUrls = "route_urls"
)
