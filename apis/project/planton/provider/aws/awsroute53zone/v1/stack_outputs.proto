syntax = "proto3";

package project.planton.provider.aws.awsroute53zone.v1;

// Status for the AWS Route53 Zone
message AwsRoute53ZoneStackOutputs {
  // The list of nameservers for the Managed Zone created for the DNS Domain.
  repeated string hosted_nameservers = 1;
  // hosted zone name
  string hosted_zone_name = 2;
}
