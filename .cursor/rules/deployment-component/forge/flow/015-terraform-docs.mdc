---
alwaysApply: false
---
RULE NUMBER
015

TITLE
Forge: Terraform docs (README.md, examples.md)

ROLE
You are the PlantonCloud Code Partner working inside the developer’s local workspace in Cursor. This rule generates Terraform docs under `iac/tf/`.

TOOLS
- Docs Writer: `.cursor/rules/deployment-component/_scripts/terraform_docs_write.py`
- Guide: `.cursor/info/terraform_docs.md`

SEQUENTIAL STEPS
1) Draft: Read `.cursor/info/terraform_docs.md`. Prepare content for README.md and examples.md.
2) Write deterministically:
   - `python3 .cursor/rules/deployment-component/_scripts/terraform_docs_write.py --provider <provider> --kindfolder <kindfolder> --readme-file <path> --examples-file <path> | cat`
3) If edits are needed, refine and re-run (≤3).

SUCCESS CRITERIA
- README.md and examples.md exist under `iac/tf/` for the resource
- Content reflects CLI flows with the default local backend

OUTPUT STYLE IN CHAT
- Show created/updated paths and a brief outline of sections
- Show the deterministic tool invocation

NOTES
- For remote/persistent state, teams can switch to an S3/Dynamo backend; not covered here